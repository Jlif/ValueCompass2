# 工作日志: 2026-03-26（续）

## 完成事项（续）

9. **PyInstaller 打包脚本**
   - 创建 `python/build.py` - PyInstaller 打包脚本
     - 支持 `--onefile` 单文件模式
     - 支持 `--onedir` 目录模式（默认，启动更快）
     - 自动收集所有 hidden imports（aktools, akshare, uvicorn, fastapi 等）
     - 平台自动检测（macos/windows/linux）
     - 输出到 `src-tauri/bin/<platform>/`

   - 创建 `python/aktools_server.py` - 入口包装脚本
     - 处理 `--port` 和 `--host` 参数
     - 信号处理（SIGINT/SIGTERM 优雅关闭）
     - 支持 PyInstaller 打包后的路径解析

   - 创建 `python/requirements.txt` - Python 依赖
   - 创建 `python/README.md` - 详细使用文档
   - 创建 `python/.gitignore` - Python 构建产物忽略

10. **Tauri 配置更新**
    - 更新 `tauri.conf.json` 添加 `externalBin: ["bin/aktools"]`
    - 更新 `python_service.rs` 优化可执行文件查找逻辑
      - 支持 Tauri sidecar 路径
      - 支持平台特定子目录（bin/macos/, bin/windows/, bin/linux/）
      - 更好的错误提示

## 新增文件

```
python/
├── .gitignore           # Python 构建产物忽略
├── README.md            # 打包使用文档
├── requirements.txt     # Python 依赖
├── aktools_server.py    # 入口包装脚本（66行）
└── build.py             # PyInstaller 打包脚本（178行）
```

## 修改文件

- `src-tauri/tauri.conf.json` - 添加 externalBin 配置
- `src-tauri/src/python_service.rs` - 优化查找逻辑
- `docs/progress.md` - 标记任务完成

## 使用方法

```bash
# 1. 安装依赖
cd python
pip install -r requirements.txt

# 2. 打包
python build.py

# 3. 验证
../src-tauri/bin/macos/aktools --port=8080
```

## 打包输出

- 默认输出到 `src-tauri/bin/<platform>/aktools`
- 目录模式约 200-400MB（包含 Python 运行时 + AKShare）
- 首次打包耗时 5-10 分钟

## 下一步

1. 前端股票列表展示页面
2. 集成 lightweight-charts 显示 K 线图
