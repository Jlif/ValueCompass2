# 工作日志: 2026-03-27

## 完成事项

### 1. 生产构建打包
- [x] 修复 PyInstaller 入口脚本 (`python/aktools_server.py`)
  - 使用动态导入 `aktools.main` 替代不存在的 `get_default_application`
  - 使用 `importlib.util` 加载 aktools 模块
- [x] 配置 Tauri sidecar
  - 更新 `tauri.conf.json` 添加 `externalBin`
  - 复制打包后的 aktools 到 `src-tauri/bin/`

### 2. 动态端口管理
- [x] 实现端口自动分配（从 18080 开始查找可用端口）
- [x] 检测已存在服务并复用（避免重复启动）
- [x] 修复服务健康检测（改用 `/version` 端点）

### 3. 进程生命周期管理
- [x] 实现 `stop()` 方法通过系统命令停止服务
- [x] 处理跨 await 点的 MutexGuard 问题
- [x] 应用退出时自动停止服务

### 4. 前端交互优化
- [x] 添加"停止服务"按钮
- [x] 实现按钮状态动态变化（启动/停止/启动中...）
- [x] 添加禁用状态样式

### 5. 复盘文档
- [x] 创建 POST-003: Tauri + AKTools 打包调试复盘
- [x] 更新复盘索引 README.md

## 关键决策

| 决策 | 原因 |
|------|------|
| 使用 `/version` 检测服务 | 根路径 `/` 返回 500 |
| 通过 `lsof` 停止进程 | 无法保存检测到的服务进程句柄 |
| 单文件模式打包 | Tauri sidecar 需要单个可执行文件 |

## 代码统计

```
7 files changed, 200+ insertions, 50+ deletions
```

## 待办

- lightweight-charts 图表展示
- UI 美化
