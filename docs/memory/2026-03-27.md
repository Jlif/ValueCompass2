# 工作日志: 2026-03-27

## 完成事项

### 1. 修复 AKTools API 集成问题
- [x] 修复 API URL 路径（添加 `/public/` 前缀）
  - `stock_info_a_code_name` - 股票列表接口
  - `stock_zh_a_daily` - K 线数据接口
- [x] 前端日期格式化（`2026-03-26T00:00:00.000` → `2026-03-26`）
- [x] 添加调试日志帮助定位问题

### 2. 优化应用启动流程
- [x] 启动时自动检测数据库是否为空
- [x] 数据库为空时自动同步股票列表（5493 只）
- [x] 添加服务启动失败容错处理

### 3. 前端交互优化
- [x] "同步股票列表"按钮移除服务状态限制
- [x] 点击同步时自动尝试启动服务

### 4. 复盘文档
- [x] 创建 POST-002: AKTools API 404 错误与数据同步问题
- [x] 更新复盘索引 README.md

## 关键决策

| 决策 | 原因 |
|------|------|
| 使用 `stock_info_a_code_name` 替代 `stock_zh_a_spot_em` | 原接口在代理环境下无法访问 |
| 使用 `stock_zh_a_daily` 替代 `stock_zh_a_hist` | 原接口在代理环境下无法访问 |
| 前端格式化日期 | 后端返回 ISO 格式，展示层处理更合适 |
| 启动时自动同步 | 用户首次使用无需手动操作 |

## 遇到的问题

1. **API 404 错误**
   - 现象：点击同步按钮界面无变化
   - 原因：Rust 代码 URL 缺少 `/public/` 路径
   - 解决：修正 URL 路径

2. **代理导致接口无法访问**
   - 现象：`stock_zh_a_spot_em` 和 `stock_zh_a_hist` 接口报错
   - 原因：系统代理设置影响 Python requests
   - 解决：切换为可用接口（`stock_info_a_code_name`, `stock_zh_a_daily`）

## 代码统计

```
10 files changed, 778 insertions(+), 217 deletions(-)

新增目录:
- python/          # PyInstaller 打包脚本
- src-tauri/bin/   # sidecar 占位文件
- src/components/  # 前端组件
- src/hooks/       # React hooks
- src/types/       # TypeScript 类型

新增文档:
- docs/postmortem/POST-002-aktools-404-error.md
- docs/memory/2026-03-26-2.md
- docs/memory/2026-03-26-3.md
```

## 下一步

- [ ] lightweight-charts 图表展示优化
- [ ] 生产构建测试（PyInstaller 打包 Python 服务）
- [ ] 完善错误处理和日志记录
